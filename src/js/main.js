"use strict";

var App = App || {};

(function() {
  // set up objects to hang MVC off of
  App.models = {};
  App.views = {};
  App.controllers = {};

  // set up constants
  App.NUM_TIMESTEPS = 121; // 0 to 120 (exclude 0 as it has no data)
  App.NUM_RUNS = 20;

  App.init = function() {
    
  };
})();
